using System.Collections.Generic;
using MediatR;

namespace HR.Application.Companies;

public record GetCompaniesQuery(string? Keyword) : IRequest<List<CompanyDto>>;

public record GetCompanyByIdQuery(int Id) : IRequest<CompanyDto?>;
